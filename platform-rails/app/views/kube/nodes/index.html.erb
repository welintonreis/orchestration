<% page_title "Nodes" %>
<% page_subtitle "Nodes do cluster" %>

<%# Nodes + the metrics-server probe are two serial API calls. Load it
    behind a skeleton so the header paints at once — see
    Kube::NodesController#rows. %>
<turbo-frame id="kube-nodes-content" refresh="morph" target="_top" src="<%= kube_rows_nodes_path %>">
  <%= render Ui::SkeletonComponent.new(:table,
        columns: [ "w-40", "w-16", "w-20", "w-32" ], rows: 4) %>
</turbo-frame>
