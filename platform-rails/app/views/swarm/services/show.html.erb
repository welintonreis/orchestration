<% page_title "Serviço Swarm" %>
<% page_subtitle params[:id] %>

<% content_for :page_icon do %><%= sidebar_icon(:services).html_safe %><% end %>

<% content_for :title_actions do %>
  <%= link_to swarm_services_path, class: "inline-flex items-center gap-2 bg-surface-inset hover:bg-surface-active text-text-secondary text-sm font-medium px-3 py-2 rounded-lg border border-border-subtle transition-colors" do %>
    <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18"/></svg>
    Serviços
  <% end %>
<% end %>

<%# service + service_tasks + nodes in series before the first byte, and the
    task list grows with the service's whole history. The real name shows in
    the hero once the frame lands — see Swarm::ServicesController#body. %>
<turbo-frame id="service-content" refresh="morph" src="<%= body_swarm_service_path(params[:id]) %>">
  <div class="flex gap-5 items-start">
    <div class="flex-1 min-w-0 space-y-4">
      <%= render Ui::SkeletonComponent.new(:detail) %>
      <%= render Ui::SkeletonComponent.new(:detail) %>
      <%= render Ui::SkeletonComponent.new(:table,
            columns: [ "w-16", "w-32", "w-20", "w-24", "w-28" ], rows: 6) %>
    </div>
    <div class="w-52 flex-shrink-0 hidden lg:block">
      <%= render Ui::SkeletonComponent.new(:detail) %>
    </div>
  </div>
</turbo-frame>
