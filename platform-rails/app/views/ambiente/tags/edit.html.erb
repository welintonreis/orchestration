<% content_for :title, "Editar Tag" %>
<div class="max-w-lg mx-auto">
  <div class="flex items-center gap-2 text-sm text-gray-500 mb-4">
    <%= link_to "Tags", ambiente_tags_path, class: "hover:text-gray-300" %> <span>/</span> <span class="text-gray-300">Editar</span>
  </div>
  <div class="bg-gray-900 border border-gray-800 rounded-xl p-6">
    <h2 class="text-lg font-semibold text-white mb-5">Editar Tag: <%= @tag.name %></h2>
    <%# Model is EnvironmentTag but route is ambiente_tags — same mismatch
        as ambiente/registries, explicit url: needed. %>
    <%= form_with model: @tag, url: ambiente_tag_path(@tag), local: true do |f| %>
      <div class="mb-4">
        <%= f.label :name, "Nome", class: "block text-xs font-medium text-gray-400 mb-1" %>
        <%= f.text_field :name, class: "w-full bg-gray-800 border border-gray-700 rounded-lg px-3 py-2 text-sm text-gray-100 focus:outline-none focus:ring-1 focus:ring-cyan-500" %>
      </div>
      <div class="mb-5">
        <%= f.label :color, "Cor", class: "block text-xs font-medium text-gray-400 mb-1" %>
        <div class="flex items-center gap-3">
          <%= f.color_field :color, class: "w-12 h-10 rounded-lg bg-gray-800 border border-gray-700 cursor-pointer p-0.5" %>
          <%= f.text_field :color, id: "tag_color_hex", class: "flex-1 bg-gray-800 border border-gray-700 rounded-lg px-3 py-2 text-sm font-mono text-gray-100 focus:outline-none focus:ring-1 focus:ring-cyan-500" %>
        </div>
      </div>
      <div class="flex gap-3">
        <%= f.submit "Salvar", class: "btn-primary" %>
        <%= link_to "Cancelar", ambiente_tags_path, class: "btn-secondary" %>
      </div>
    <% end %>
  </div>
</div>
