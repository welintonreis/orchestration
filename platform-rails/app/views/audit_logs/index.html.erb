<% page_title "Audit Log" %>

<% content_for :title_actions do %>
  <form class="flex items-center gap-2" method="get">
    <select name="user_id" class="px-2 py-1.5 bg-gray-800 border border-gray-700 rounded-lg text-sm text-gray-200 focus:outline-none focus:border-cyan-600">
      <option value="">All users</option>
      <% @users.each do |u| %>
        <option value="<%= u.id %>" <%= "selected" if params[:user_id] == u.id.to_s %>><%= u.email_address %></option>
      <% end %>
    </select>
    <input type="text" name="action_filter" value="<%= params[:action_filter] %>"
           placeholder="Filter action…"
           class="w-40 px-3 py-1.5 bg-gray-800 border border-gray-700 rounded-lg text-sm text-gray-200 placeholder-gray-600 focus:outline-none focus:border-cyan-600">
    <button type="submit" class="px-3 py-1.5 text-sm bg-gray-700 hover:bg-gray-600 text-gray-200 rounded-lg transition-colors">Filter</button>
    <%= link_to "Clear", audit_logs_path, class: "text-sm text-gray-500 hover:text-gray-300 transition-colors" if params[:user_id].present? || params[:action_filter].present? %>
  </form>
<% end %>

<div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden">
  <% if @audit_logs.empty? %>
    <div class="flex flex-col items-center justify-center py-16 text-center">
      <p class="text-gray-400">No audit log entries found.</p>
    </div>
  <% else %>
    <table class="w-full text-sm">
      <thead>
        <tr class="border-b border-gray-800 text-left">
          <th class="px-4 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Time</th>
          <th class="px-4 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">User</th>
          <th class="px-4 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Action</th>
          <th class="px-4 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Target</th>
          <th class="px-4 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">IP</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-800/60">
        <% @audit_logs.each do |log| %>
          <tr class="hover:bg-gray-800/40 transition-colors">
            <td class="px-4 py-3 text-xs text-gray-500 whitespace-nowrap">
              <%= log.created_at.strftime("%Y-%m-%d %H:%M:%S") %>
            </td>
            <td class="px-4 py-3 text-sm text-gray-300">
              <%= log.user&.email_address || "—" %>
            </td>
            <td class="px-4 py-3">
              <span class="font-mono text-xs text-cyan-400"><%= log.action %></span>
            </td>
            <td class="px-4 py-3 text-xs text-gray-400">
              <% if log.target_type.present? %>
                <span class="text-gray-500"><%= log.target_type %></span>
                <span class="font-mono text-gray-400 ml-1"><%= log.target_id&.first(12) %></span>
              <% end %>
              <% if log.metadata.present? && log.metadata != {} %>
                <div class="text-gray-600 mt-0.5">
                  <% log.metadata.each do |k, v| %>
                    <span class="mr-2"><%= k %>: <%= v %></span>
                  <% end %>
                </div>
              <% end %>
            </td>
            <td class="px-4 py-3 text-xs font-mono text-gray-600">
              <%= log.ip_address || "—" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <div class="px-4 py-3 border-t border-gray-800 flex items-center justify-between">
      <span class="text-xs text-gray-600">
        Showing <%= (@page - 1) * 50 + 1 %>–<%= [(@page * 50), @total].min %> of <%= @total %>
      </span>
      <div class="flex items-center gap-2">
        <% if @page > 1 %>
          <%= link_to "← Prev", audit_logs_path(page: @page - 1, user_id: params[:user_id], action_filter: params[:action_filter]),
                class: "px-3 py-1 text-xs bg-gray-800 hover:bg-gray-700 text-gray-400 hover:text-gray-200 rounded border border-gray-700 transition-colors" %>
        <% end %>
        <% if @page * 50 < @total %>
          <%= link_to "Next →", audit_logs_path(page: @page + 1, user_id: params[:user_id], action_filter: params[:action_filter]),
                class: "px-3 py-1 text-xs bg-gray-800 hover:bg-gray-700 text-gray-400 hover:text-gray-200 rounded border border-gray-700 transition-colors" %>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
