local_env:
  name: local
  endpoint_type: unix
  endpoint: "unix:///var/run/docker.sock"
  active: true

tcp_env:
  name: remote
  endpoint_type: tcp
  endpoint: "tcp://10.0.0.1:2376"
  active: false
